<h1>Your Readers</h1>
<ul>
  <% @readers.each do |reader| %>
    <li class="index-links"> <%= link_to "#{reader.username}", user_reader_path(id: reader.id) %> </li>
  <% end %>
</ul>

<p>Add a <%= link_to 'new Reader', new_user_reader_path(id: @user.id) %>.</p>

